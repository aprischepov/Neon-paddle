import Foundation

enum AppConstants {
    enum Legal {
        static let privacyPolicyURL = URL(string: "https://neon-paddle.app/privacy-policy")!
        static let termsOfUseURL = URL(string: "https://neon-paddle.app/terms-of-use")!
    }
}
